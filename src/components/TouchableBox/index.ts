import { TouchableOpacity } from 'react-native';
import styled from 'styled-components/native';

// the touchable used in all app, if need change the native component, we have to change only here
// I prefer to use ReactButton from gesture handler, but I think this is not the chalange focus
export const TouchableBox = styled(TouchableOpacity)`
  
`;
